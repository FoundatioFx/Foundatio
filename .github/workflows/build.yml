name: Build
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.100
    - name: Build Reason
      run: echo ${{github.ref}} and ${{github.event_name}}
    - name: Build
      run: dotnet build --configuration Release --nologo
    - name: Run Tests
      run: dotnet test --configuration Release --no-build --nologo --results-directory /github/home/artifacts --logger:trx
    - name: Package
      if: github.event_name != 'pull_request'
      run: dotnet pack --configuration Release --output /github/home/artifacts
    - name: Push Packages
      if: github.event_name != 'pull_request'
      run: ./push.sh ${{ secrets.FEEDZ_KEY }}
    - name: Artifacts
      uses: actions/upload-artifact@v1.0.0
      with:
        name: packages
        path: /github/home/artifacts
