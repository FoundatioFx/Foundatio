<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <Description>Test harnesses for Foundatio messaging and jobs: run the real message bus over a recording in-memory transport, await quiescence, and assert on the messages that were sent, published, handled, retried, or dead-lettered; drive the in-memory job runtime deterministically without the pump.</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Foundatio\Foundatio.csproj" />
  </ItemGroup>
</Project>
